"""One spec module per aeroplane, `build_aircraft_type.py <key>` drives.

Each module below is data, not mechanism: it names one aeroplane's part stems, wheel-bone
groups, angle source and six performance blocks, and carries that aeroplane's own docstring
prose - the WHY, moved verbatim from the build_plane<N>_type.py it replaces, not dropped. See
`Tools/Python/build_aircraft_type.py`'s module docstring for what the mechanism does with it.
"""
